<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tiny.system.mapper.SysMenuMapper">

    <!-- 根据用户ID查询菜单列表 -->
    <select id="selectMenusByUserId" resultType="com.tiny.system.entity.SysMenu">
        SELECT DISTINCT m.menu_id, m.menu_name, m.parent_id, m.sort, m.path, m.component,
               m.menu_type, m.visible, m.status, m.perms, m.icon,
               m.create_by, m.create_time, m.update_by, m.update_time, m.del_flag, m.remark
        FROM sys_user_role ur
        INNER JOIN sys_role_menu rm ON ur.role_id = rm.role_id
        INNER JOIN sys_menu m ON rm.menu_id = m.menu_id
        WHERE ur.user_id = #{userId}
          AND m.menu_type IN ('M', 'C')
          AND m.status = '0'
          AND m.del_flag = '0'
        ORDER BY m.parent_id, m.sort
    </select>

    <!-- 根据用户ID查询权限标识列表 -->
    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT DISTINCT m.perms
        FROM sys_user_role ur
        INNER JOIN sys_role_menu rm ON ur.role_id = rm.role_id
        INNER JOIN sys_menu m ON rm.menu_id = m.menu_id
        WHERE ur.user_id = #{userId}
          AND m.status = '0'
          AND m.del_flag = '0'
          AND m.perms IS NOT NULL
          AND m.perms != ''
    </select>

</mapper>